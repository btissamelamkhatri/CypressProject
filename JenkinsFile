println "env.APP_NAME is ${env.APP_NAME}"
pipeline {

   agent any
   options { ansiColor('xterm') }
   stages{
      stage('setup') {
         steps {
            bat 'npm i'
            bat 'npx cypress run'
            }
         }
      }
    }