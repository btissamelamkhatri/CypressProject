pipeline {

   agent any

   stages{
      stage('setup') {
         steps {
            bat 'npm i'
            bat 'set NO_COLOR=1 ' 
            bat 'npx cypress run'
            }
         }
      }
    }