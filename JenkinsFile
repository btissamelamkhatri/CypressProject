pipeline {
   agent any
   stages {
      stage('setup') {
         steps {
            bat 'npm i'
            bat 'npx cypress run --browser chrome cypress/e2e/1-getting-started/*.cy.js'
            }
         }
      }
    }