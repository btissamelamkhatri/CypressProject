pipeline {
   agent any
   options {
      ansiColor('xterm')
   }
   stages {
      stage('setup') {
         steps {
            bat 'npm i'
            bat 'npx NO_COLOR=1 cypress run'
            }
         }
      }
    }